# Root .htaccess for API Routing
# Purpose: Route /api/* requests to /church-api/public/
# This allows frontend to call https://elchurch.site/api/... 
# while backend is physically located at /church-api/public/

<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect /api/* requests to /church-api/public/
    # This makes the backend accessible at the root domain
    RewriteRule ^api/(.*)$ church-api/public/$1 [L,PT]
    
    # Also support /church-api direct access (for testing)
    RewriteRule ^church-api/(.*)$ church-api/public/$1 [L,PT]
</IfModule>

# Gzip compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/x-httpd-php
    AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>
